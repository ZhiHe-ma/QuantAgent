# QuantAgent 项目方案：超级拼装量化工具

版本：V1.0 · 平台定位修订｜日期：2026-09-21
项目：[ZhiHe-ma/QuantAgent](https://github.com/ZhiHe-ma/QuantAgent)
本次交付：项目方案。本文功能分阶段建设，不代表已经实现；本次不修改代码、不提交 GitHub。

## 1. 一句话定位

**QuantAgent 是一台“量化工具主机”：把成熟开源能力封装成可选插件，按工作流组合使用，并统一管理数据、结果和兼容性。**

用户要做研究、比较模型、回测策略或生成报告，不必分别操作一堆工具；选择一套适合的组合，在一个入口运行、查看和追溯。

“研究审计层”是平台的重要底座，但不是最终产品的全部。最终形态是“可拼装的量化工作台”，不是重新开发行情系统、训练框架和回测引擎。

### 用 Switch 打比方

| 比喻 | QuantAgent 中对应什么 | 负责什么 |
| --- | --- | --- |
| 主机 | 核心 Core | 识别插件、检查能否配合、安排执行、收集结果 |
| 游戏卡带 | 能力插件 Plugin | 提供行情、模型、训练、回测、报告等具体能力 |
| 卡槽标准 | 插件协议 Contract | 约定输入输出、数据含义、版本和错误 |
| 组合套餐 | 工作流配方 Recipe | 规定哪些插件按什么顺序配合 |
| 卡带目录 | 插件目录 Registry | 展示能力、运行要求、兼容范围和验证状态 |
| 存档与体检 | 数据、审计和测试底座 | 保存输入输出、支持重放、发现升级破坏 |

这个比喻有一个边界：开源项目本身不等于现成卡带。**卡带 = 开源能力 + 适配器 + 运行配置 + 兼容测试。** 安装依赖、填写密钥、下载数据或准备算力，有时仍不可省略。

## 2. 用户最终能得到什么

产品使用流程：选择任务和套餐，检查条件，运行插件组合，查看统一结果；需要时替换其中一个已兼容插件。

例如：“比较两个模型对同一批新闻的判断，并在七天后看结果。”

用户选择数据范围、两个模型和评价期限；平台固定输入，分别运行，保存模型和 Prompt 版本，生成对照报告，再按约定回填结果。早期用配置或表单，后期再用自然语言生成待确认的配方。

| 新增功能 | 大白话用途 | 范围与先后 |
| --- | --- | --- |
| 插件管理 | 要什么装什么，不用的关闭；查看缺少什么配置 | 第一阶段做精选插件，不先做开放市场 |
| 工作流配方 | 保存并重复运行“取数—分析—评价—报告”套餐 | 先做固定验证过的套餐，再开放组合 |
| 统一结果视图 | 不同工具的结果用一致入口查看和导出 | 原生结果同时保留，不能强行抹平差异 |
| 数据置信度报告 | 看数据是否完整、及时、异常，来源是否冲突 | 第一阶段提供质量与血缘；历史校准随后补齐 |
| 历史重放 | 用同一批旧输入检查升级后哪里变了 | 优先离线重放，重新调用模型单独计费和标记 |
| 多来源比较 | 比较同资产、同时间口径的数据差异 | 有两个真实数据适配器后开放 |
| 结果回填与评价 | 检验当时判断在约定期限后的表现 | 独立功能建设，不把接口通过当作预测有效 |
| 实验追踪与模型比较 | 记录谁、用什么数据和参数、得到什么结果 | 优先复用 MLflow |
| 漂移与周报 | 看输入、输出和表现是否变化，形成持续报告 | 在历史样本和评价流程稳定后加入 |
| 兼容报告 | 清楚展示哪些插件、版本和组合经过验证 | 从第一套套餐开始持续生成 |

自然语言不是“说一句就任意执行”。它先生成执行方案，展示插件、输入、费用与权限，再按用户授权运行。研究、回测和模拟是默认范围；实盘交易是未来单独审批的能力。

## 3. 现有项目怎么接进来

沿用 2026-09-20 已核对的代码基线：[main · 1efa1406be64a0bfd22f0d735a5ecab6b4ab1aa6](https://github.com/ZhiHe-ma/QuantAgent/commit/1efa1406be64a0bfd22f0d735a5ecab6b4ab1aa6)。本次方案修订未重新运行代码或核对新的提交。

| 当前已有 | 升级处理 |
| --- | --- |
| RSS/Atom、清洗去重、新闻评级与规则校准 | 先封装现有入口，逐步形成新闻和分析插件 |
| Daily/Monitor、行情、历史记忆、DeepSeek | 保留现有业务；作为第一套内置业务配方迁移 |
| Markdown、企业微信及发送结果确认 | 分成报告生成与消息投递能力，权限分开 |
| 同日幂等、强制重跑、有限重试 | 保留行为并加入套餐级回归 |
| SQLite 审计及 JSON/Markdown 历史数据 | 作为兼容基线和只读数据入口 |
| signal_outcomes 表 | 表已存在，自动回填与评价仍需开发 |
| DRY_RUN | 阻止部分业务副作用，但仍可能联网和调用模型，不能当离线重放 |
| 43 项既有 unittest | 沿用上次本地通过记录；不代表外部项目已集成通过 |

上次基线在 Python 3.12.14 上运行，网络和模型依赖主要通过模拟验证。这次新增插件与平台能力均属于规划，不能标记为已实现。

迁移采用“先包起来，再逐步拆开”：不要求第一步重写现有主程序，也不让安装研究插件改变原有 Daily/Monitor 的依赖环境。

证据：[README](https://github.com/ZhiHe-ma/QuantAgent/blob/1efa1406be64a0bfd22f0d735a5ecab6b4ab1aa6/README.md)、[审计模块](https://github.com/ZhiHe-ma/QuantAgent/blob/1efa1406be64a0bfd22f0d735a5ecab6b4ab1aa6/signal_audit.py)、[数据库结构](https://github.com/ZhiHe-ma/QuantAgent/blob/1efa1406be64a0bfd22f0d735a5ecab6b4ab1aa6/sql/001_signal_audit.sql)。

## 4. 最终架构：小核心、厚插件、统一底座

| 架构部分 | 负责什么 | 不负责什么 |
| --- | --- | --- |
| 使用入口 | 命令行、配置；后续加入界面和自然语言选配 | 不把界面当作量化引擎 |
| 配方层 | 声明步骤、输入输出、参数、版本和失败处理 | 不默认任意插件都能串联 |
| 核心层 | 查插件目录、校验组合、交接数据、启动执行、记录状态 | 不自己重写训练和回测框架 |
| 插件层 | 封装成熟工具，也容纳现有 QuantAgent 能力 | 不直接绕过权限读写所有数据 |
| 运行层 | 管理任务进程、依赖环境、超时、资源和取消 | 不把所有依赖装进一个 Python 环境 |
| 数据与审计底座 | 契约、快照、血缘、结果、运行记录和历史重放 | 不用一个总分代替所有证据 |
| 测试与权限保障 | 插件验收、组合验收、版本限制、密钥和读写范围 | 不以“可安装”代替“可兼容” |

一次任务的主路径是：**用户选配方 → 执行前检查 → 按配方调用插件 → 保存原生与标准结果 → 评价、报告和审计。**

插件目录为选配和检查提供信息；数据契约贯穿步骤间交换；权限与兼容测试覆盖整套流程。原始生产数据保持私有，不因新增插件自动上传外部服务。

### 4.1 核心只做必要的连接工作

自有开发集中在：插件清单、能力匹配、数据语义、薄适配器、执行前检查、运行记录，以及兼容验收。

成熟基础能力尽量复用：

- Python 插件发现可采用 entry points（包声明的插件入口）；QuantAgent 另外维护静态清单与允许加载名单。[Python 官方打包指南](https://packaging.python.org/en/latest/guides/creating-and-discovering-plugins/)
- 工作流执行可接成熟后端；Prefect 是候选，已有状态记录、重试和超时机制。第一套小流程可先顺序执行，不必先开发通用调度平台，也不必要求所有用户部署额外服务。[Prefect Flows](https://docs.prefect.io/v3/concepts/flows)
- 表格检查、实验追踪、模型研究与漂移检测使用对应成熟项目。QuantAgent 只维护自己的业务口径和接线规则。

后端选择须通过实际部署和依赖测试后确定；方案中的候选不等于兼容承诺。

### 4.2 插件采用不同运行方式

| 插件情况 | 建议运行方式 | 边界 |
| --- | --- | --- |
| 轻量、可信、依赖兼容 | 主进程内加载 | 加载代码就可能执行，发现插件不等于允许加载 |
| 依赖重或版本冲突 | 独立 Python 环境和进程 | 隔离依赖，不等于安全沙箱 |
| 不同语言、独立服务 | 子进程、容器或明确的服务接口 | 交换有版本的数据，不共享任意内部对象 |
| 来源不明或权限要求过大 | 不自动加载，先审核 | 容器也必须另设文件、网络和资源限制 |

每次执行设定超时、资源和输出目录。只对可安全重试的步骤重试；发消息、下单、写外部系统等步骤必须单独处理幂等与确认，不能笼统“失败就重跑”。

卸载插件不删除历史运行和原始产物；已停用插件不能再被新配方自动调用。

## 5. 卡带标准：怎么做到能换、能接、能查

### 5.1 每张卡带有一份说明书

插件清单至少包括：

| 内容 | 说明 |
| --- | --- |
| 身份与版本 | 插件 ID、插件版本、上游版本、支持的契约版本 |
| 能力 | 取行情、预测、回测、评价或报告；一个项目可以包装为多个能力 |
| 输入与输出 | 数据类型、字段含义、单位、时间和市场口径 |
| 运行要求 | Python/系统、依赖、内存、可选硬件、启动方式 |
| 权限与成本 | 是否联网、读写哪些路径、需何种密钥、是否调用付费接口 |
| 失败处理 | 错误类型、超时、是否可重试、幂等规则 |
| 验证信息 | 已测版本组合、测试报告、日期、限制和维护状态 |
| 许可信息 | 项目许可证、数据授权与再分发约束，发布前核验 |

最初使用版本库中的精选目录即可，不急着建设公开插件市场。目录状态至少区分“已验证、实验性、未验证、停用”；未知版本不能自动显示为兼容。

### 5.2 统一接口，不等于所有数据塞进一张表

按能力定义几类数据包，共享标识、时间、版本和血缘：

| 数据包 | 装什么 | 特别注意 |
| --- | --- | --- |
| 行情与新闻 | 价格、成交量、正文、来源与时间 | 发布、采集、可用时间分开；记录复权、币种和市场 |
| 预测与信号 | 分数、方向、期限、模型输出 | 分数、概率、置信度不是同一个量 |
| 策略与组合 | 持仓目标、仓位、调仓、成本和风控规则 | 预测不能自动变成可执行策略 |
| 回测与评价 | 收益、交易、风险指标、评价标签 | 记录基准、费用、滑点、交易日历和样本区间 |
| 运行与报告 | 参数、版本、输入引用、结果引用、日志 | 保存原生结果、转换规则和信息损失 |

小型元数据可用 JSON；较大表格可用约定的列式文件格式。步骤间优先传文件引用和哈希，第一阶段无需引入大型消息系统。核心 run_id 与 MLflow 等外部 ID 保存映射，不假设二者相同。

共同规则：

- 时间必须带时区；保留事件时间与当时可获得数据的时间。日期字段另行声明日历含义。
- 收益交换统一采用小数并明确命名；旧字段按已核实口径转换，不凭 return_pct 等名称猜测。
- BTC/USD 与 BTC/USDT 分开；不把不同市场、不同复权方式的价格当同一数据。
- 空值统一为 null，并记录原因；未知不补成零或虚构精确时间。
- contract_version、来源、代码/模型/Prompt 版本及数据集哈希可追溯；无法恢复的信息明确标记。
- 当前 finalized_at 只代表判断完成时刻，不能拿来冒充原始决策时刻。
- 价格、收益和性能比较使用约定容差；不要求所有后端数值完全一致。

OpenBB 的“同一个标准模型对应多个 Provider”可作为接口设计参考，但不是直接引入整个项目的理由。[OpenBB 标准化框架](https://docs.openbb.co/odp/python/developer/standardization)

### 5.3 什么叫“自由拼装”

统一接口减少反复写接线代码，但不能消除业务差异。系统应给出三类结果：

1. 直接兼容：接口、语义和版本均匹配，且目标套餐通过验证。
2. 需要转换：明确插入转换插件，展示转换与信息损失，再验证。
3. 不兼容或未验证：指出缺少的字段、口径或证据，不悄悄继续。

例如 Qlib 预测分数不能直接塞给 Freqtrade 就称为完成回测。中间还需明确策略、仓位、交易品种、时间对齐和费用规则。两张卡带分别通过测试，也不等于它们的组合已通过。

## 6. 优先复用什么、先提供哪些套餐

### 6.1 插件接入顺序

| 能力 | 候选项目或现有模块 | 建议接入方式 | 优先级 |
| --- | --- | --- | --- |
| 现有业务和历史数据 | QuantAgent SQLite/JSON、Daily/Monitor、Markdown | 封装当前能力，历史只读入口先行 | 首批 |
| 数据结构与质量检查 | Pandera | 声明业务 Schema，调用成熟验证能力 | 首批 |
| 参数、指标和产物追踪 | MLflow | 可选本地或独立后端；验证写入与读回 | 首批 |
| 研究结果交换与研究流程 | Qlib | 先接一个真实结果格式，再接独立研究配方 | 第二批 |
| 多数据源 | 现有来源；按缺口考虑 OpenBB | 各来源独立适配，先统一实际使用字段 | 第二批 |
| 策略回测 | Freqtrade 或 LEAN | 按目标市场先选一个，独立运行并导出结果 | 后续，不同时硬接两个 |
| 漂移与评价报告 | Evidently | 对接标准数据表、参考集和评价配置 | 评价流程稳定后 |

Pandera 提供 Schema、检查与懒验证；MLflow 支持参数、代码版本、指标和产物追踪；Qlib 覆盖金融研究工作流。插件仍需对应版本与接口的实际验证。[Pandera](https://pandera.readthedocs.io/en/stable/)、[MLflow Tracking](https://mlflow.org/docs/latest/ml/tracking/)、[Qlib](https://github.com/microsoft/qlib)

回测引擎按需求选择，不提前承诺统一支持所有市场；漂移报告可复用 Evidently。[Freqtrade Backtesting](https://www.freqtrade.io/en/stable/backtesting/)、[LEAN](https://github.com/QuantConnect/Lean)、[Evidently](https://docs.evidentlyai.com/introduction)

### 6.2 四套计划套餐

| 套餐 | 插件组合 | 用户拿到什么 |
| --- | --- | --- |
| 历史数据体检 | SQLite/JSON 只读入口 → Pandera → 报告及可选 MLflow | 缺失、时效、异常、来源和版本报告 |
| 日报与判断复盘 | 现有行情/新闻 → 分析模型 → 审计 → Markdown；推送另行启用 | 可追溯日报、模型对比及后续结果评价 |
| 模型研究 | 合规数据集 → Qlib 研究流程 → 评价 → MLflow/报告 | 同一数据下的研究结果和实验对照 |
| 策略回测 | 明确策略与数据 → 所选回测引擎 → 标准结果 → 对比报告 | 带费用、时间和市场口径的回测结果 |

第一套先用 SQLite 和对应 JSON 快照实现同类数据入口的替换：只改配置，后面的检查和报告不用改。这比先接十个项目更能证明平台的可拼装能力。

后两套先各自闭环，只有确有需求且策略语义明确时，才建设“跨研究引擎与回测引擎”的组合。

## 7. 可靠性底座：能接上，也要知道结果靠不靠谱

### 7.1 置信度拆开显示

至少分成来源历史可靠性、数据质量、跨来源一致性、模型自报置信度、历史校准五项。

没有多来源时，一致性显示“无法评价”；没有足够历史样本时，历史校准显示“样本不足”。模型自报的 80 分不能直接解释成 80% 命中率；置信度分桶也要先明确预测目标和评价期限。

综合分可以作为展示，但分项、样本量、计算规则和适用范围必须可展开查看。当前质量评分不自动继承为“数据真实概率”。

### 7.2 四个月历史数据怎么用

以实际可提供的历史记录为准，先盘点覆盖范围，不把“四个月”直接等同于完整训练或评价数据集。

- 私有保存原始生产数据；公开测试用合成或确认可发布的脱敏样本。
- 固定一批 Golden Fixtures（黄金样本），保存版本、哈希和脱敏方法。
- 记录当时的输入、输出和可用时间；旧记录缺失的输入不能靠今天的网络数据冒充恢复。
- 离线重放用于检查读取、转换、判断流程和报告是否回归；禁用网络、模型调用和消息推送。
- 重新推理属于新实验，记录新模型、Prompt、参数及费用；非确定性输出按约定指标比较，不要求逐字相同。
- 迁移旧数据库先在副本演练，验证记录数、约束、可读性及备份恢复后再决定实施。

### 7.3 回填的定义先说清楚

T+1/T+3/T+7 必须说明按自然日还是交易日。Crypto 可按决策后 24/72/168 小时；其他市场按选定交易日历定义，两者不能混用。

回填记录决策时间、参考价、目标时间、实际取价时间、价格口径和缺失原因；不够条件的记录标记不可评价。没有策略与成本设定时，方向命中率只是判断评价，不代表可交易收益。

### 7.4 兼容验收覆盖三层

| 验收对象 | 必须证明什么 |
| --- | --- |
| 单插件 | 安装与运行要求明确；正常、缺字段、错类型、扩展字段、超时等行为可预期 |
| 插件连接处 | 标的、单位、时间、日历、预测期限与可用时间对齐，必要转换被记录 |
| 整套配方 | 固定输入能跑通；输出与基线符合容差；中断、失败、取消、重试及权限符合预期 |

另加历史格式、数据库迁移演练和故障测试：RSS 返回 HTML、行情缺失/重复/倒序、非法模型 JSON、SQLite 锁定、上游改字段、断网与磁盘写入失败。

CI（持续集成）从首个插件接入时建立：核心离线回归、真实依赖测试、完整套餐测试分开报告；在线检查另列，不能用模拟接口通过冒充真实联调。

兼容报告至少记录：插件与上游精确版本、契约版本、Python、系统、实际依赖、套餐版本、提交 SHA、数据哈希、测试日期、通过/失败/跳过与已知限制。

首批以既有 Python 3.12 环境建立固定 Linux 组合，再扩展 Windows 和其他版本。**测试过一个版本，不宣称整个版本区间或全部组合通过。** 对外称项目自测兼容报告，不暗示上游官方认证。

## 8. 后续建设路线

按可交付结果推进，不按学习课程安排，也不在未经估算时承诺工期。

| 阶段 | 建什么 | 做到什么才进入下一步 |
| --- | --- | --- |
| 第一阶段：主机能跑一套套餐 | 冻结基线；最小插件清单、契约和执行入口；SQLite/JSON + Pandera + 报告，MLflow 可选；同步建立 CI | 真正跑通历史体检；可换同类数据入口；不改原始数据，不影响旧 Daily |
| 第二阶段：现有能力可组合 | 包装 Daily/Monitor、模型、报告；配方配置、版本检查和依赖隔离；历史重放、回填评价 | 至少两套明确配方通过端到端验收；换兼容模型或输出插件只改配置 |
| 第三阶段：扩展研究和回测 | Qlib 先结果交换再研究套餐；选一个回测引擎；多来源比较与模型对照 | 各自真实运行；输入输出语义明确；依赖冲突和失败不拖垮核心 |
| 第四阶段：使用体验接近“插卡” | 精选插件目录、安装与升级提示、配方模板、统一界面；按需加入漂移和周报 | 用户能看清支持范围、成本和缺失配置；升级可回退到已验证组合 |
| 第五阶段：受控自由拼装 | 配方编辑、转换建议、自然语言生成草案；社区插件审核与发布规范 | 未验证组合明确标记；高权限步骤需确认；无法兼容时能解释原因 |

实盘交易不作为前几阶段默认目标。未来若加入，应单独定义账户权限、风控、模拟验证、限额、人工确认和停机机制；“想要什么直接用”不能变成“未经确认自动动用资金”。

### 先冻结哪些规范

沿用原来的三份规范，并补上平台所需的两份：

| 计划产物 | 作用 |
| --- | --- |
| QUANTAGENT_DATA_CONTRACT.md | 统一字段、时间、单位、空值与血缘 |
| QUANTAGENT_PLUGIN_CONTRACT.md | 插件清单、能力、权限、生命周期与错误 |
| QUANTAGENT_RECIPE_SPEC.md | 插件连接、参数、版本、输入输出与失败处理 |
| COMPATIBILITY_POLICY.md | 支持范围、破坏性变更、版本退役和声明规则 |
| COMPATIBILITY_TEST_MATRIX.md | 单插件、连接处、整套配方和环境矩阵 |

这些是后续实施交付，不在本次方案修订中生成或编写代码。第一阶段只定实际套餐需要的协议，保留扩展点，不一次设计所有未来金融场景。

## 9. 边界与完成标准

发布插件前核验所选版本的许可证、NOTICE、数据授权及集成方式；进程隔离解决运行问题，不能替代许可核验。生产密钥按插件最小授权提供，不写入报告、配方或公开夹具。

“不重复造轮子”不是“不写代码”。QuantAgent 仍需开发适配器、契约、配方、语义转换和测试；训练、回测等成熟通用能力尽量复用。

升级成功应体现为：

1. 用户能从一个入口运行经过验证的量化套餐，而不只是查看开源项目链接。
2. 同类兼容插件能通过配置替换，不必修改核心业务代码。
3. 新增重型插件不强迫所有用户安装其依赖，也不破坏现有运行。
4. 每次结果能查到实际数据、插件、模型、Prompt、版本和转换过程。
5. 历史数据能读、流程能重放，升级差异与不兼容原因有证据。
6. 失败不会被包装成成功；未知、未测、信息损失与样本不足明确显示。

**最终目标：用成熟开源项目提供能力，用 QuantAgent 把能力接起来、管起来、验清楚，让用户按需组合使用。**
